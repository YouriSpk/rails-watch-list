<div class="cover mb-4 d-flex align-items-center justify-content-center">
  <h1>Create some new lists</h1>
</div>
<ul>
  <% @lists.each do |list| %>
    <li>
      <%= link_to list.name, list_path(list) %>
    </li>
  <% end %>
</ul>
<%= link_to "+ Add new list", new_list_path %>
